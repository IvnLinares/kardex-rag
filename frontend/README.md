# Frontend — Kardex RAG

Vue 3 (Composition API) + Vite + TypeScript.

Ver el [README del repo](../README.md) para setup, scripts de calidad
(`lint`/`format`/`build`) y contexto general del proyecto.
